# Dataset root placeholder

Do not commit raw datasets to GitHub. Set the dataset root with:

```bash
export PAAE_DATA_ROOT=/path/to/datasets
```

See `docs/DATASETS.md` for the expected dataset structure.
