# Checkpoints

Put trained PAAE checkpoints here, for example:

- `PAAE_CUB.pth`
- `PAAE_DOGS.pth`
- `PAAE_FLOWERS.pth`

Large checkpoint files should be released through GitHub Releases, Zenodo, Google Drive, OneDrive, or another stable storage service rather than committed directly to Git.
